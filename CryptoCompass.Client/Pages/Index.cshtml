@page "/"
@inject AppState state

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

<i>@(state.Loading ? "State loading..." : string.Empty)</i>

@functions {
    protected override async Task OnInitAsync()
    {
        await state.Init();
    }
}